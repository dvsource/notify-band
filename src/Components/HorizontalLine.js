import React from 'react';
import {View} from 'react-native';

export const HorizontalLine = () => {
  return (
    <View
      style={{
        borderTopColor: 'black',
        borderTopWidth: 1,
        marginTop: 10,
      }}
    />
  );
};
